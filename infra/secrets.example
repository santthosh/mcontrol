# Secret Manager Keys
#
# These secrets should be created in Google Cloud Secret Manager
# for each environment (dev, staging, prod):
#
# gcloud secrets create SECRET_NAME --data-file=-
#
# Grant access to the Cloud Run service account:
#
# gcloud secrets add-iam-policy-binding SECRET_NAME \
#   --member="serviceAccount:mcontrol-api-ENV@PROJECT_ID.iam.gserviceaccount.com" \
#   --role="roles/secretmanager.secretAccessor"

# Required Secrets:

google-client-id
  Description: Google OAuth 2.0 client ID (one per environment)
  Format: NNNNN.apps.googleusercontent.com
  Setup: Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client IDs
  Note: Each environment needs its own client with the correct redirect URI:
    dev:     https://dev-api.mcontrol.ai/api/auth/google/callback
    staging: https://staging-api.mcontrol.ai/api/auth/google/callback
    prod:    https://api.mcontrol.ai/api/auth/google/callback

google-client-secret
  Description: Google OAuth 2.0 client secret (paired with client ID above)
  Format: GOCSPX-xxx

# Optional Secrets:

anthropic-api-key
  Description: Anthropic API key for Claude
  Format: sk-ant-xxx

openai-api-key
  Description: OpenAI API key
  Format: sk-xxx

google-ai-api-key
  Description: Google AI API key
  Format: xxx
